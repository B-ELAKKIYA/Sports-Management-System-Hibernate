<?xml version="1.0" encoding="UTF-8"?>

<hibernate-configuration>
    <session-factory>

        <!-- Oracle Dialect (Hibernate 6) -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.OracleDialect
        </property>

        <!-- Oracle JDBC Driver -->
        <property name="hibernate.connection.driver_class">
            oracle.jdbc.OracleDriver
        </property>

        <!-- Oracle XE Connection -->
        <property name="hibernate.connection.url">
            jdbc:oracle:thin:@localhost:1521:xe
        </property>

        <!-- Database Credentials -->
        <property name="hibernate.connection.username">ELAKKIYA</property>
        <property name="hibernate.connection.password">newpassword</property>

        <!-- Hibernate Settings -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.hbm2ddl.auto">update</property>
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Entity Mapping -->
        <mapping class="com.ground.entity.GroundSlot"/>
        <mapping class="com.ground.entity.SlotUsage"/>

    </session-factory>
</hibernate-configuration>
